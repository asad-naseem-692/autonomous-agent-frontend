# Feature: Tool Result Display (UI)
**Owner:** Frontend | **Module:** Agent Conversation

## Goal
Show what the agent found when it used a read tool (e.g. customer
lookup, order details), inline in the conversation.

## Scope
- A small structured card inside the chat thread showing key-value
  results from a read tool call (e.g. customer name, balance) — not just
  raw text.
