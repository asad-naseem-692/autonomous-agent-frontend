# Feature: Admin Execution Logs (UI)
**Owner:** Frontend | **Module:** Admin Panel

## Goal
Let an admin audit actions taken across the whole system.

## Scope
- Table/list of execution log entries with filters (user, tool name,
  status, date range). Visible only to admin role.
