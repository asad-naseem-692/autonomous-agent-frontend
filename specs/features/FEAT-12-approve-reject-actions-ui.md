# Feature: Approve / Reject Actions (UI)
**Owner:** Frontend | **Module:** Human Approval Workflow

## Goal
Let the user act on a pending approval.

## Scope
- "Approve" and "Reject" buttons on the approval card (FEAT-11).
- On click, call the respective backend endpoint, then update the card
  to show the resolved state and continue the conversation.
