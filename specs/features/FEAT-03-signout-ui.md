# Feature: Sign Out (UI)
**Owner:** Frontend | **Module:** Authentication

## Goal
Let a logged-in user log out cleanly.

## Scope
- "Logout" button in shared header. Clears token, redirects to `/login`.
