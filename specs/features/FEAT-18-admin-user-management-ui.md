# Feature: Admin User Management (UI)
**Owner:** Frontend | **Module:** Admin Panel

## Goal
Let an admin suspend or delete an operator.

## Scope
- Suspend/reactivate and delete buttons per row (admin only), with
  confirmation dialogs. Buttons disabled for the admin's own row.
